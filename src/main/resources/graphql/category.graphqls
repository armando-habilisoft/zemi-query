extend type Query {
    categories (first: Int, after: String, last: Int, before: String, name: String): CategoryConnection
    category(id: Int, name: String): Category
}

type Category {
    id: Int!
    name: String!
    description: String
}
